        // let total =100
        // for(let i=0;i<total;i++){
        //   let midx=lerp(x2,x3,i/total)
        //   let midy=lerp(y2,y3,i/total)
        //   square(midx,midy,5)
        // }
        // line(x1,y1,x2,y2)
        // line(x3,y3,x4,y4)
        // push();
        // translate(x2, y2);
        // rotate(random(PI * 2));
        // square(0, 0, random(5, 10));
        // pop();

        push();
        translate(x3, y3);
        rotate(random(PI * 2));
        square(0, 0, random(5, 10));
        pop();
